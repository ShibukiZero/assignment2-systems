## 1.1.6(a) Active Memory Timeline Images

This directory is reserved for the two memory_viz screenshots used in `1.1.6(a)`.

Chosen representative configuration:

- model size: `2.7b`
- precision: `fp32`
- context length: `512`

Expected images:

- one active-memory timeline for `forward` / inference-only
- one active-memory timeline for `train-step` / full training step

Suggested filenames:

- `fp32_2.7b_ctx512_forward_active_memory_timeline.png`
- `fp32_2.7b_ctx512_train_step_active_memory_timeline.png`
